"""
Ablation launcher placeholder.

Recommended ablations:
  - --no_role_alpha       set role_alpha=0
  - --no_prototype        clear prototype keys/values
  - --no_gate_confidence  set gamma=beta=gate=1 in model variant
  - --dynamic_only        use role/prototype score only
  - --semantic_only       use FeatureBERT4Rec only

This file is intentionally kept as a template because the exact ablation set should
be frozen after the first full ISDDG sanity run.
"""
print(__doc__)
